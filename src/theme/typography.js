// Единый набор текстовых стилей — работает поверх обычного системного
// шрифта (кастомный шрифт не подключаем, чтобы не требовалась нативная
// пересборка приложения). "Красивее" здесь — это вес, межбуквенный
// интервал и высота строки, подобранные под разные роли текста.
// Цвет сюда не входит — добавляется отдельно через colors.xxx в каждом
// компоненте, т.к. один и тот же стиль текста используется на разных
// фонах и с разным цветом.
const typography = {
  // Крупный заголовок экрана: "Тренировка", "История", дата в DayEditor
  screenTitle: {
    fontSize: 26,
    fontWeight: '800',
    letterSpacing: 0.2,
  },
  // Заголовок раздела внутри экрана: "Мои упражнения", "Упражнения"
  sectionTitle: {
    fontSize: 17,
    fontWeight: '700',
    letterSpacing: 0.1,
  },
  // Маленький "ярлычок"-подпись над группой элементов: "Отметить день",
  // "Обновления" — капсом и с разрядкой, как в хороших интерфейсах
  label: {
    fontSize: 12,
    fontWeight: '700',
    letterSpacing: 0.8,
    textTransform: 'uppercase',
  },
  // Обычный текст: email, описания, пункты списков
  body: {
    fontSize: 16,
    fontWeight: '400',
    lineHeight: 22,
  },
  // Обычный текст, но чуть жирнее — названия упражнений в списках
  bodyBold: {
    fontSize: 16,
    fontWeight: '600',
  },
  // Второстепенный текст помельче: статусы, хинты
  caption: {
    fontSize: 13,
    fontWeight: '500',
    letterSpacing: 0.2,
  },
  // Текст на крупных кнопках
  button: {
    fontSize: 16,
    fontWeight: '700',
    letterSpacing: 0.3,
  },
  // Текст на маленьких кнопках/переключателях
  buttonSmall: {
    fontSize: 14,
    fontWeight: '700',
    letterSpacing: 0.2,
  },
  // Числа: повторения, рейтинг, коэффициенты — моноширинные цифры,
  // чтобы столбик чисел в списке визуально ровно выстраивался
  number: {
    fontSize: 16,
    fontWeight: '700',
    fontVariant: ['tabular-nums'],
  },
};

export default typography;