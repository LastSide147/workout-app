// Единый источник цветов для всего приложения. Значения — те же,
// что уже использовались по всем экранам (просто раньше hex-код
// был скопирован в каждый файл отдельно). Меняете цвет здесь — он
// обновится сразу везде, где используется этот токен.
//
// Единственное намеренное отличие от того, что было: coefficientText
// в ExerciseManagementScreen раньше был #888, здесь сведён к
// textPlaceholder (#999) — разница на глаз незаметна, зато один
// оттенок серого для "второстепенного" текста вместо двух похожих.
const colors = {
  // Основной акцентный цвет приложения (кнопки, активные вкладки, ссылки)
  primary: '#2196F3',
  // Подсветка своей строки в таблице рейтинга
  primaryLight: '#E3F2FD',

  // Семантические цвета
  success: '#4CAF50',
  warning: '#FF9800',
  danger: '#e53935',

  // Базовые
  white: '#fff',
  black: '#000',

  // Текст
  textPrimary: '#333',
  textSecondary: '#555',
  textMuted: '#777',
  textPlaceholder: '#999',

  // Границы и разделители
  border: '#ccc',
  divider: '#eee',
  dividerLight: '#f2f2f2',

  // Состояния элементов
  disabled: '#B0BEC5',

  // Цвет статуса "Выходной" (остальные статусы дня совпадают
  // с success/warning/danger выше)
  statusWeekend: '#9E9E9E',

  // Затемнение фона под модальными окнами
  overlay: 'rgba(0,0,0,0.4)',
};

export default colors;
